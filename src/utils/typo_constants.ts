import { IFormSkipLimit } from 'types';

export const DEFAULT_START: number = 0;
export const DEFAULT_LIMIT: number = 25;

export const paginate: IFormSkipLimit = {
  start: 0,
  limit: 15,
};

export const TDDA_TYPE = 'TDDA';
export const PIN = 'pin';

export const HIT_SLOP = { top: 16, left: 16, bottom: 16, right: 16 };
