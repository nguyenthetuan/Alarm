export enum ELanguage {
  VI = 'vi',
  EN = 'en',
}

export enum EnumPinType {
  ADMIN = 'admin',
  USER = 'user',
}
export enum EnumStatusRestaurant {
  OPENNING = 'openning',
  CLOSED = 'closed',
  CLOSED_UNTILL = 'closed_untill',
}

export enum EnumMenuStatus {
  FOOD_ALSO_SERVICED = 0,
  SOLD_OUT_TODAY = 1,
  STOP_PERMANENTLY = 2,
}
export enum EnumStatusLog {
  LOGIN = 'login',
  LOGOUT = 'logout',
  NEW_INSTALL = 'already',
}

export enum EnumCatalogType {
  MENU = 'MENU',
  ORDER = 'ORDER',
}

export enum EnumOTP {
  REGISTER = 'REGISTER',
  FORGOT = 'FORGOT',
}
